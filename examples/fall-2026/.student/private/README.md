# Private context

Files here are excluded from Git and from `scholarfs context` by default. The two `.example.md` files are deliberately fake so this public example can demonstrate the convention safely.

