# Courses

Each course has a stable directory named with its course code. Machine-readable metadata lives in `.student/courses.json`; `COURSE.md` holds human context.

Create a course with:

```bash
scholarfs course add CS-101 --title "Introduction to Computer Science"
```

